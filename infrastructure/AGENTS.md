# Infrastructure Instructions

- This subtree owns Docker Compose, DIAL config, auth settings, init SQL, Caddy, and infrastructure-side interceptors.
- Keep checked-in config aligned with the live docs in `docs/architecture/ARCHITECTURE.md` and `docs/plans/ROADMAP.md`.
- Be explicit about what is live versus planned when changing infrastructure docs or config.
