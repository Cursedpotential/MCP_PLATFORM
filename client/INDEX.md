# Client (React + CopilotKit) — Agent Index
> Read parent INDEX.md before this one.
> Check local TODO.md for approved tasks before starting work.

---


## Current state
Scaffolded React/Vite app with CopilotKit and shadcn/ui installed.
Not a finished UI. Do not document it as one.

## I need to work on the app shell or layout
→ `src/components/layout/`

## I need to work on evidence review pages
→ `src/pages/EvidencePage.tsx`
→ `src/pages/ConversationsPage.tsx`
→ Alpha 1 UI patterns: `MCP_Tool_Platform/` — audit before building (UI-001)

## I need to wire CopilotKit
→ `src/App.tsx` — current CopilotKit integration point
→ Requires UI-002 spec approval before modifying

## I need UI components
→ `src/components/ui/` — full shadcn/ui component library installed

## I need to check what's approved to work on
→ `TODO.md` — client task list (start with UI-001 audit)

## I need to check last session state
→ `memory/MEMORY.md`

## Rules specific to client
→ `AGENTS.md`
