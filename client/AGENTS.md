# Client Instructions

- The current client is a scaffolded React/Vite app with installed UI and CopilotKit dependencies.
- Do not document it as a finished analyst UI unless the code actually supports that.
- Keep frontend claims aligned with `client/src/App.tsx` and `docs/plans/ROADMAP.md`.
