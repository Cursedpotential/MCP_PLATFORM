{
  "name": "js-mcp-server",
  "version": "1.0.0",
  "description": "AI DIAL JavaScript SDK MCP Server for legacy JS components and fallback extractors.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "echo 'No build step required for pure JS' && exit 0",
    "start": "node src/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "latest"
  }
}
