# MCP Servers Instructions

- This subtree holds the three runtime MCP server implementations.
- Keep server-specific behavior documented in the matching local docs under `docs/wiki/tools/`.
- Read the server-local `AGENTS.md` before changing a specific server.
